import { SurveyAnswerChoice } from './survey-answer-choice';

describe('SurveyAnswerChoice', () => {
  it('should create an instance', () => {
    expect(new SurveyAnswerChoice()).toBeTruthy();
  });
});
