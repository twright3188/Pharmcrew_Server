import { SurveyAnswerListResp } from './survey-answer-list-resp';

describe('SurveyAnswerListResp', () => {
  it('should create an instance', () => {
    expect(new SurveyAnswerListResp()).toBeTruthy();
  });
});
