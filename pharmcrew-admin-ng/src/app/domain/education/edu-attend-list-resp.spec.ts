import { EduAttendListResp } from './edu-attend-list-resp';

describe('EduAttendListResp', () => {
  it('should create an instance', () => {
    expect(new EduAttendListResp()).toBeTruthy();
  });
});
