import { EducationListResp } from './education-list-resp';

describe('EducationListResp', () => {
  it('should create an instance', () => {
    expect(new EducationListResp()).toBeTruthy();
  });
});
