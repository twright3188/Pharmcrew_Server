import { PtaxnoticeListReq } from './ptaxnotice-list-req';

describe('PtaxnoticeListReq', () => {
  it('should create an instance', () => {
    expect(new PtaxnoticeListReq()).toBeTruthy();
  });
});
