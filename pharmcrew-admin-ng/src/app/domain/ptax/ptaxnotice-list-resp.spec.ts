import { PtaxnoticeListResp } from './ptaxnotice-list-resp';

describe('PtaxnoticeListResp', () => {
  it('should create an instance', () => {
    expect(new PtaxnoticeListResp()).toBeTruthy();
  });
});
